server:
  port: 8083

spring:
  application:
    name: experian-connector
  kafka:
    bootstrap-servers: localhost:9092
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics

logging:
  level:
    com.loanorigination.experian: DEBUG

# Mock Credit Score Configuration
# Configure specific credit scores for specific SSNs for testing
# Format: SSN (without dashes) -> credit score
mock:
  credit-scores:
    experian:
      # Example: "123456789": 750
      # Add your test SSNs here
      # "999887777": 650
      # "111223333": 800

